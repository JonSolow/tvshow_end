from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A model to predict when a TV show will end at any ppoint in the series.',
    author='Jon Solow',
    #license='',
)
